import { Editor } from "@tiptap/react"

const CommentSection = (props: { editor: Editor }) => {
    return <div className="absolute right-0 translate-x-full">
        test comment section
    </div>
}

export default CommentSection;